name: CodSpeed

on:
  push:
    branches: [ with-gil, without-gil ]
  # `workflow_dispatch` allows CodSpeed to trigger backtest
  # performance analysis in order to generate initial data.
  workflow_dispatch:

permissions:
  contents: read
  id-token: write # for OpenID Connect authentication with CodSpeed

jobs:
  benchmarks:
    name: Run benchmarks
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      # `with-gil` runs on the regular interpreter, `without-gil` on the
      # free-threaded one. The interpreter must be the only difference between
      # the two branches, so both pin the same allocator (see the run step).
      - name: Resolve Python version
        id: python
        env:
          # The branch being built decides the interpreter.
          BRANCH: ${{ github.ref_name }}
        run: |
          case "$BRANCH" in
            without-gil)
              version="3.14.7t"
              ;;
            *)
              version="3.14.7"
              ;;
          esac
          echo "version=$version" >> "$GITHUB_OUTPUT"

      - name: Set up uv
        uses: astral-sh/setup-uv@v7
        with:
          python-version: ${{ steps.python.outputs.version }}
          enable-cache: true

      - name: Install dependencies
        run: uv sync --locked

      # Both branches use `mimalloc`: the free-threaded build rejects the
      # `malloc` allocator CodSpeed sets by default, and mixing allocators
      # would add an allocator difference on top of the GIL difference
      # (measured here at -18% to +11% on the same interpreter).
      - name: Run benchmarks
        uses: CodSpeedHQ/action@v5
        with:
          mode: simulation
          run: env PYTHONMALLOC=mimalloc uv run pytest tests/ --codspeed
